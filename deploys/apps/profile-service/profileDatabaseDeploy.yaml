apiVersion: v1
kind: Service
metadata:
  name: profiledb
  namespace: blog
spec:
  selector:
    app: profile-service-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: profile-service-postgres
  labels:
    app: profile-service-postgres
  namespace: blog
spec:
  replicas: 1
  selector:
    matchLabels:
      app: profile-service-postgres
  template:
    metadata:
      labels:
        app: profile-service-postgres
    spec:
      containers:
      - name: postgres
        image: postgres:18
        ports:
        - containerPort: 5432
          name: postgres
        env:
          - name: POSTGRES_USER
            valueFrom:
              secretKeyRef:
                name: profile-db
                key: username
          - name: POSTGRES_DB
            valueFrom:
              secretKeyRef:
                name: profile-db
                key: url
          - name: POSTGRES_PASSWORD
            valueFrom:
              secretKeyRef:
                name: profile-db
                key: password
        volumeMounts:
          - mountPath: /var/lib/postgresql/18/data
            name: postgres-data
      volumes:
        - name: postgres-data
          emptyDir: {}